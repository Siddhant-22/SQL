SHOW DATABASES;
USE SAKILA;
SELECT * FROM CITY;
SELECT * FROM COUNTRY;

#INNER JOIN
SELECT CITY.CITY_ID,CITY.COUNTRY_ID,COUNTRY.LAST_UPDATE FROM CITY
INNER JOIN COUNTRY ON CITY.COUNTRY_ID = COUNTRY.COUNTRY_ID;

#LEFT JOIN
SELECT CITY.CITY_ID,COUNTRY.COUNTRY_ID,CITY.LAST_UPDATE,COUNTRY.LAST_UPDATE FROM CITY
LEFT JOIN COUNTRY ON CITY.COUNTRY_ID = COUNTRY.COUNTRY_ID;

SELECT CITY.CITY_ID,COUNTRY.COUNTRY_ID FROM CITY
LEFT JOIN COUNTRY ON CITY.LAST_UPDATE = COUNTRY.LAST_UPDATE;

#RIGHT JOIN
SELECT CITY.CITY_ID,COUNTRY.COUNTRY_ID,CITY.LAST_UPDATE,COUNTRY.LAST_UPDATE FROM CITY
RIGHT JOIN COUNTRY ON CITY.COUNTRY_ID = COUNTRY.COUNTRY_ID;


CREATE DATABASE SID;
USE SID;
CREATE TABLE CUSTOMER(
CUSTOMER_ID INT,
CUSTOMER_NAME VARCHAR(30)
);
SELECT * FROM CUSTOMER;
INSERT INTO CUSTOMER VALUES
(1,'JOHN'),
(2,'ROBERT'),
(3,'DAVID'),
(4,'JOHN'),
(5,'BETTY')
SHOW DATABASES;

CREATE TABLE ORDERS(
CUSTOMER_ID INT,
ORDER_ID INT,
ORDER_AMOUNT INT
);

SELECT * FROM ORDERS;

INSERT INTO ORDERS VALUES
(10,1,200),
(3,2,500),
(6,3,300),
(5,4,800),
(8,5,150)

#INNER JOIN
SELECT CUSTOMER.CUSTOMER_ID,CUSTOMER.CUSTOMER_NAME,ORDERS.ORDER_AMOUNT FROM CUSTOMER 
INNER JOIN ORDERS ON CUSTOMER.CUSTOMER_ID = ORDERS.CUSTOMER_ID

#LEFT JOIN
SELECT CUSTOMER.CUSTOMER_ID,CUSTOMER.CUSTOMER_NAME,ORDERS.ORDER_AMOUNT FROM CUSTOMER 
LEFT JOIN ORDERS ON CUSTOMER.CUSTOMER_ID = ORDERS.CUSTOMER_ID

#RIGHT JOIN
SELECT ORDERS.CUSTOMER_ID,CUSTOMER.CUSTOMER_NAME,ORDERS.ORDER_AMOUNT FROM CUSTOMER 
RIGHT JOIN ORDERS ON CUSTOMER.CUSTOMER_ID = ORDERS.CUSTOMER_ID


#THREE JOIN
CREATE TABLE STUDENT(
STUDENT_ID VARCHAR(30),
FIRST_NAME VARCHAR(30),
LAST_NAME VARCHAR(30)
);

SELECT * FROM STUDENT;

INSERT INTO STUDENT VALUES
(1,'SHREYA','BAIN'),
(2,'RIANA','FOSTER'),
(3,'YOSEF','NAYLOR');

CREATE TABLE STUDENT_COURSE(
STUDENT_ID VARCHAR(30),
COURSE_ID VARCHAR(30)
);

SELECT * FROM STUDENT_COURSE;

INSERT INTO STUDENT_COURSE VALUES
(1,2),
(1,3),
(2,1),
(2,2),
(2,3),
(3,1);

CREATE TABLE COURSE(
STUDENT_ID VARCHAR(30),
COURSE_NAME VARCHAR(30),
TEACHER_ID VARCHAR(30)
);

SELECT * FROM COURSE;

INSERT INTO COURSE VALUES
(1,'DATABASE DESIGN',1),
(2,'ENGLISH LIT',2),
(3,'PYTHON',1);

#INNER JOIN 1 AND 2
SELECT STUDENT.STUDENT_ID,STUDENT.FIRST_NAME,STUDENT.LAST_NAME,STUDENT_COURSE.COURSE_ID FROM STUDENT
INNER JOIN STUDENT_COURSE ON STUDENT.STUDENT_ID = STUDENT_COURSE.STUDENT_ID;

#INNER JOIN 2 AND 3
SELECT STUDENT_COURSE.STUDENT_ID,STUDENT_COURSE.COURSE_ID,COURSE.COURSE_NAME,COURSE.TEACHER_ID FROM STUDENT_COURSE
INNER JOIN COURSE ON STUDENT_COURSE.STUDENT_ID = COURSE.STUDENT_ID;

#LEFT JOIN 1 AND 2
SELECT STUDENT.STUDENT_ID,STUDENT.FIRST_NAME,STUDENT.LAST_NAME,STUDENT_COURSE.COURSE_ID FROM STUDENT
LEFT JOIN STUDENT_COURSE ON STUDENT.STUDENT_ID = STUDENT_COURSE.STUDENT_ID;

#RIGHT JOIN 1 AND 2
SELECT STUDENT_COURSE.STUDENT_ID,STUDENT.FIRST_NAME,STUDENT.LAST_NAME,STUDENT_COURSE.COURSE_ID FROM STUDENT
RIGHT JOIN STUDENT_COURSE ON STUDENT.STUDENT_ID = STUDENT_COURSE.STUDENT_ID;

#LEFT JOIN 2 AND 3
SELECT STUDENT_COURSE.STUDENT_ID,STUDENT_COURSE.COURSE_ID,COURSE.COURSE_NAME,COURSE.TEACHER_ID FROM STUDENT_COURSE
LEFT JOIN COURSE ON STUDENT_COURSE.STUDENT_ID = COURSE.STUDENT_ID;

#RIGHT JOIN 2 AND 3
SELECT COURSE.STUDENT_ID,STUDENT_COURSE.COURSE_ID,COURSE.COURSE_NAME,COURSE.TEACHER_ID FROM STUDENT_COURSE
RIGHT JOIN COURSE ON STUDENT_COURSE.STUDENT_ID = COURSE.STUDENT_ID;

# INNER JOIN 1.2.3
SELECT STUDENT.STUDENT_ID,STUDENT.FIRST_NAME,STUDENT.LAST_NAME,STUDENT_COURSE.COURSE_ID,COURSE.COURSE_NAME,COURSE.TEACHER_ID FROM STUDENT
JOIN STUDENT_COURSE ON STUDENT.STUDENT_ID = STUDENT_COURSE.STUDENT_ID
JOIN COURSE ON COURSE.STUDENT_ID = STUDENT_COURSE.STUDENT_ID;


